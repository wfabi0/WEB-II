info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:80/alunos/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
