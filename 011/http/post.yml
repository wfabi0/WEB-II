info:
  name: post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:80/alunos
  body:
    type: json
    data: |-
      {
        "nome_alu": "Fábio",
        "nota_alu": 99,
        "cpf_alu": "104.517.910-88"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
