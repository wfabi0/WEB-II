info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:80/alunos
  body:
    type: json
    data: |-
      {
        "id": "1",
        "nome_alu": "Fábio Emanuell",
        "nota_alu": 99,
        "cpf_alu": "104.517.910-88"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
